const { authroutes } = require("./register");
const { products } = require("./products");
const { purchagesroutes } = require("./purchages");
module.exports = {
  authroutes,
  products,
  purchagesroutes,
};
